echo "Scripts Loaded!!! (1/2)"

// Shortcut for disconnet
alias "ds" "disconnect"

// Netgraph inside Scoreboard
alias "+scorenet" "+showscores; net_graphheight 0"
alias "-scorenet" "-showscores; net_graphheight 9999"
bind "TAB" "+scorenet" //Bind to whatever you prefer (Default TAB)

//Change Sens
alias sen "sen_hi"
alias sen_hi "sensitivity 2; playvol buttons\\blip1 0.5; alias sen sen_lo"
alias sen_lo "sensitivity 1.1; playvol buttons\\blip2 0.5; alias sen sen_hi"
bind mouse3 sen

// Utility Crosshair Toggle
bind mouse4 "toggle cl_crosshairsize 2 1000"

// Change r_drawtracer_firstperson
alias showtracer "showtracer_y"
alias showtracer_y "r_drawtracers_firstperson 1; playvol buttons\\blip1 0.5; alias showtracer showtracer_n"
alias showtracer_n "r_drawtracers_firstperson 0; playvol buttons\\blip2 0.5; alias showtracer showtracer_y"
bind mouse4 showtracer

// Display Advanced Damage Details
alias displaydamage "displaydamage_on"
alias displaydamage_on "con_filter_text Damage Given To; con_filter_text_out Player:; con_filter_enable 2; developer 1; playvol buttons\\blip1 0.5; alias displaydamage "displaydamage_off""
alias displaydamage_off "con_filter_enable 0; developer 0; playvol buttons\\blip2 0.5; alias displaydamage "displaydamage_on""
bind "F5" "displaydamage" //Bind to whatever you prefer (Default F5)

// Clear Decals
bind mouse5 r_cleardecals

// Jump Throw (For Smokes)
alias "+jumpthrow" "+jump;-attack"
alias "-jumpthrow" "-jump"

// Easy Bomb Find
alias +bombfind "+use;gameinstructor_enable 1;cl_clearhinthistory"
alias -bombfind "-use;gameinstructor_enable 0;cl_clearhinthistory"
bind "e" "+bombfind"

// Fast Switch
alias +fastswitch slot3
alias -fastswitch lastinv

// crouch jump & mousewheel jump
alias +cjump "+jump; +duck"
alias -cjump "-jump; -duck"
bind "n" "+cjump"

// skate
alias +ska "+left; +moveleft"
alias +skd "+right; +moveright"
alias -ska "-left; -moveleft"
alias -skd "-right; -moveright"
alias skate "bind a +ska; bind d +skd"
alias nskate "bind a +moveleft; bind d +moveright"

// inverse
alias mova "bind w +forward; bind a +moveleft; bind s +back; bind d +moveright;"
alias movb "bind s +forward; bind d +moveleft; bind w +back; bind a +moveright;"

echo "Scripts Loaded!!! (2/2)"
